<%= form_for(@google) do |f| %>
  <% if @google.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@google.errors.count, "error") %> prohibited this google from being saved:</h2>

      <ul>
      <% @google.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %><br />
    <%= f.text_area :query %>
  </div>
  <div class="field">
    <%= f.label :query_rank %><br />
    <%= f.text_area :query_rank %>
  </div>
  <div class="field">
    <%= f.label :query_number %><br />
    <%= f.number_field :query_number %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
